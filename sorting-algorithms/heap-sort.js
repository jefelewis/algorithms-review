/*
Notes:
- Works with unsorted arrays
*/

/*
Example:
let array = [10, 4, 2, 5, 2, 1, 9, 0];
*/


// Heap Sort
const heapSort = (array) => {
  try {

  }
  catch (error) {
    console.log(error);
  }
};
